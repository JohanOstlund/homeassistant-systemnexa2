{
  "title": "System Nexa 2",
  "config": {
    "step": {
      "user": {
        "title": "Add System Nexa 2 device",
        "description": "Enter IP/hostname and port (default 3000). Token can be left empty.",
        "data": {
          "host": "IP address or hostname",
          "port": "Port",
          "token": "Token",
          "name": "Name"
        }
      }
    }
  }
}